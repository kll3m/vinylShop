{% extends 'base.html.twig' %}

{% block title %}Style{% endblock %}

{% block body %}
    <h1>Style</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ style.id }}</td>
            </tr>
            <tr>
                <th>NomStyle</th>
                <td>{{ style.nomStyle }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('style_index') }}">Retour à la listet</a>

    <a href="{{ path('style_edit', {'id': style.id}) }}">Editer</a>

    {{ include('style/_delete_form.html.twig') }}
{% endblock %}
