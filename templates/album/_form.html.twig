

<div class="container">
        <div class="row">
                <div class="formNew">
                        {{ form_start(form) }}
                        {{ form_widget(form) }}
                <button type="button" class="btn btn-primary">{{ button_label|default('Sauvegarder') }}</button>
                        {{ form_end(form) }}
                </div>

        </div>
</div>

