{{ form_start(form) }}
    {{ form_widget(form) }}
    <p>Ajouter une image</p>
    <button class="btn">Charger une image</button>
    <button class="btn">{{ button_label|default('Sauvegarder') }}</button>
{{ form_end(form) }}
