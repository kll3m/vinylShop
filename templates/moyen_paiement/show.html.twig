{% extends 'base.html.twig' %}

{% block title %}MoyenPaiement{% endblock %}

{% block body %}
    <h1>MoyenPaiement</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ moyen_paiement.id }}</td>
            </tr>
            <tr>
                <th>LibelleMoyenPaiement</th>
                <td>{{ moyen_paiement.libelleMoyenPaiement }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('moyen_paiement_index') }}">back to list</a>

    <a href="{{ path('moyen_paiement_edit', {'id': moyen_paiement.id}) }}">edit</a>

    {{ include('moyen_paiement/_delete_form.html.twig') }}
{% endblock %}
