<form method="post" action="{{ path('etat_commande_delete', {'id': etat_commande.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_method" value="DELETE">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ etat_commande.id) }}">
    <button class="btn">Delete</button>
</form>
